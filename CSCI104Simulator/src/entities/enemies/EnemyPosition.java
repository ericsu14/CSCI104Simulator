/** Defines the region of the playing field where this enemy would initially spawn
 *  before flying over to their origin point. */
package entities.enemies;

public enum EnemyPosition 
{
	kLeft, kRight
}
